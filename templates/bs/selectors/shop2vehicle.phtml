<form id="vehicle2shop" method="POST" action="<?php echo $view->getFullLink(); ?>">
    <div class="row">
        <div class="col-lg-12">
            <div class="form-group">
                <label><?php echo $view->translate('VEHICLE_2_SHOP_LABEL'); ?></label>
                <input type="hidden" name="vehicle2shop_id" value="<?php echo $activeVehicle['sid']; ?>"/>
                <select name="shop2vehicle_shop" multiple="multiple" class="form-control shop2vehicle-id">
            <?php
                foreach($allShops->get() as $shop) {
            ?>
                <option value="<?php echo $shop['sid']; ?>" <?php echo ($assignedShops->current()['shop_id'] == $shop['sid']) ? 'selected="selected"' : ''; ?>><?php echo $shop['description']; ?></option>
            <?php
                }
            ?>
                </select>
            </div>
        </div>
        <div class="col-lg-12">
            <button type="submit" name="save_shop2vehicle" value="save_shop2vehicle" class="btn btn-default"><span class="glyphicon glyphicon-ok"></span> <?php echo $view->translate('SAVE'); ?></button>
        </div>
    </div>
</form>
